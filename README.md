# Assignment 1
## Main.java
Sanzhar Yermekov
>Description: Explanation of all functions
```java
findMin(n, arr); // finding minimum number from array nums

findAverage(n, arr); // finding average from array nums

isNumberPrime(n); // returns true if number is prime

findFact(n); // find factorial of given number

findFib(n); // find number in Fibonacci sequence

findPow(a, n); // function that find a power of n

getReverse(n, nums); // returning string that contains sequence of array in reverse order

isFullDigit(line); // returns true if given string contains full digits

findBinom(n, k); // finding binomial coefficent by two numbers

findGCD(a, b); // finding gcd of two numbers
```
